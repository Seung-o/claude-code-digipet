{
  "name": "claude-digi-buddy",
  "version": "1.0.0",
  "description": "Digimon Tamagotchi that grows with your Claude Code usage",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "setup": "node scripts/setup.js",
    "uninstall": "node scripts/uninstall.js",
    "postinstall": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
